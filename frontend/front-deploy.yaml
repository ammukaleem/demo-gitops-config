apiVersion: apps/v1
kiind: Deployment
metadata:
  labels:
    app: frontend
  name: frontend
  namespace: nodeapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - image: dockaleem/frontend:build-13008769461
        name: nodeapp
        resources: {}
